#include<stdio.h>
#include<math.h>
int main()
{
    float n;
    scanf("%f",&n);
    printf("ceil = %.2f\n",ceil(n));
    printf("floor = %.2f\n",floor(n));
    printf("sqrt= %.2f\n",sqrt(n));
    printf("abs= %d\n",abs(n));
}
